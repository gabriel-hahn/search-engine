const assert = require('assert');
const Site = require('../../src/models/Site');

describe('Site model tests', () => {
    describe('Smoke tests', () => {
        it('Should have the correct structure', () => {

        });
    });
    describe('CRUD tests', () => {
        it('Should save a new site', () => {
            /*let newSite = new Site({ url: 'http://localhost:8080', title: 'My computer', description: 'Just for tests', clicks: 0 });
            assert.ok(newSite);*/
        });
    });
});
